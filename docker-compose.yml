version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://cueapi:password@db:5432/cueapi
      REDIS_URL: redis://redis:6379
      SESSION_SECRET: local-dev-secret-key-change-in-production
      ENV: development
      ALLOW_REGISTER: "true"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: >
      sh -c "python -m alembic upgrade head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  poller:
    build: .
    environment:
      DATABASE_URL: postgresql+asyncpg://cueapi:password@db:5432/cueapi
      REDIS_URL: redis://redis:6379
      SESSION_SECRET: local-dev-secret-key-change-in-production
    depends_on:
      - api
    command: python -m worker.poller

  worker:
    build: .
    environment:
      DATABASE_URL: postgresql+asyncpg://cueapi:password@db:5432/cueapi
      REDIS_URL: redis://redis:6379
      SESSION_SECRET: local-dev-secret-key-change-in-production
    depends_on:
      - api
    command: python -m arq worker.main.WorkerSettings

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cueapi
      POSTGRES_USER: cueapi
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cueapi"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
